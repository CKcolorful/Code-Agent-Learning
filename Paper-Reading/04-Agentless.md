# Agentless：复杂自主循环未必是最优解

论文：[Agentless: Demystifying LLM-based Software Engineering Agents](https://arxiv.org/abs/2407.01489)

官方代码：[OpenAutoCoder/Agentless](https://github.com/OpenAutoCoder/Agentless)

## 一句话结论

仓库级修复可以被拆成固定的“分层定位 → 多候选修复 → 测试与投票选择”流水线；当模型的长程决策能力有限时，结构化 pipeline 可能比复杂 autonomous loop 更便宜、更稳定、更容易解释。

## 1. 这篇论文为什么重要

SWE-agent 的直觉是通过更好的 ACI 释放自主 agent 的能力。Agentless 提出一个必要的反问：我们真的需要让 LLM 自己决定每一步做什么吗？

自主循环的成本不只来自模型调用，还来自决策误差的累积：一次错误搜索会带来错误文件，一次错误编辑会制造新的报错，随后 agent 可能耗费多轮修补自己造成的问题。Agentless 把不确定性集中在几个明确阶段，由外部程序控制流程。

它不是“完全不用 LLM”，而是**不用 LLM 充当自由行动的控制器**。LLM 仍负责语义判断、定位、生成 patch 和生成复现测试。

## 2. 总体流程

```text
Issue + Repository
        |
        v
1. Localization
   文件 -> 符号 -> 精确编辑位置
        |
        v
2. Repair
   多组位置 x 多个采样 -> 候选 Search/Replace patches
        |
        v
3. Patch Validation
   回归测试 + 生成的复现测试 + 规范化多数投票
        |
        v
Final Patch
```

关键思想是将端到端难题拆成可测量的中间变量：相关文件是否还在候选集合中、上下文缩小了多少、候选 patch 中是否包含正确答案、验证器能否把正确 patch 排到第一。

## 3. 阶段一：分层 Fault Localization

### 第一步：文件级定位

Agentless 把仓库压缩成类似 `tree` 的目录结构，让 LLM 根据 issue 选择可疑文件。因为文件名和目录关系可能不足，它还做 embedding 检索：先过滤明显无关目录，再把剩余文件切块，与 issue 向量计算相似度。最后合并 prompting 和 embedding 两路结果。

论文消融中，单独 prompt 定位到 ground-truth 文件的比例为 78.7%，单独 embedding 为 67.7%，组合后为 81.7%。这说明语义检索和基于项目结构的推断捕获了不同信号。

### 第二步：符号级定位

对候选文件不直接塞入全文，而是构造 skeleton：类、函数、变量声明，类字段、方法签名，以及有限的模块/类注释。模型先根据骨架选择相关类与函数。

这个压缩非常关键。论文报告把上下文从完整候选文件的 3,000 行以上压到 800 行以下，同时定位表现反而更好。长上下文如果主要是噪声，只会增加成本和混淆。

### 第三步：编辑点定位

再向模型提供上一步相关符号的完整代码，让它输出具体函数、类或行范围。由粗到细的层级化设计，避免在巨大仓库上直接做一次“猜最终行号”的高难动作。

## 4. 阶段二：多候选 Repair

Agentless 在编辑位置周围加入有限上下文，然后让模型生成 **Search/Replace diff**：

```text
SEARCH:
原始、应唯一匹配的代码片段

REPLACE:
替换后的代码片段
```

相比重写整个函数或文件，这种格式改动小、token 少、应用逻辑清晰，也更不容易引入无关幻觉。

系统不会押注一次生成。它会采样多组位置，并对每组位置生成多个 patch。论文默认的“4 组位置 × 每组 10 个 patch”在 SWE-bench Lite 上优于把所有位置合并到一个大上下文。原因是不同定位假设保持独立，能形成真正有差异的候选。

## 5. 阶段三：Patch Validation

SWE-bench 的隐藏 bug 测试在推理时不可见，仓库现有测试又多为回归测试。Agentless 因此让 LLM 根据 issue 生成多个独立复现测试，并要求它们能区分：

- 原始仓库：输出 `Issue reproduced`；
- 修复后仓库：输出 `Issue resolved`；
- 测试自身异常：输出 `Other issues`。

候选复现测试先在原始代码上执行，不能真正复现 issue 的直接淘汰。候选 patch 则先按回归测试失败数量过滤，再用复现测试筛选，最后把 AST 规范化后的 patch 做多数投票，减少空格、注释等表面差异对投票的影响。

论文消融很直观：仅多数投票解决 77/300；加入回归测试后 81/300；再加入生成的复现测试后达到 96/300。验证与选择带来的增益，和继续改进生成本身同样重要。

## 6. 结果应该怎样解读

论文 v2 报告 Agentless + GPT-4o 在 SWE-bench Lite 上解决 96/300，即 32.0%，平均成本约 0.70 美元；在论文比较范围内，它是当时表现最强的开源方案之一，并显著低于许多自主 agent 的成本。

但不要把论文标题理解为“agent 已被证明无用”：

- pipeline 的每个阶段仍在调用 LLM；
- 结果集中于 SWE-bench Lite，任务相对更自包含；
- 固定流程不擅长需要动态探索、复杂环境配置或跨工具调查的任务；
- 复现测试可能错误地编码 issue，测试通过也可能是假阳性；
- 论文显示所有候选 patch 的理论覆盖可达 42%，最终却只选中 32%，说明 selector 仍是瓶颈。

更准确的结论是：**自主性不是默认越多越好；能由可靠程序结构化的部分，不应浪费模型去反复决策。**

## 7. 对 benchmark 的反思

作者还人工分类 SWE-bench Lite，发现部分问题存在描述不足、误导性建议或近似答案泄漏。他们构造了过滤后的 Lite-S 子集。这提醒我们，系统分数可能同时测量 agent 能力和数据质量；当任务描述本身不可解时，再复杂的规划也不会产生可靠修复。

## 8. 与 SWE-agent 的正确对照

| 维度 | SWE-agent | Agentless |
| --- | --- | --- |
| 控制流 | LLM 每轮决定下一动作 | 程序固定阶段 |
| 优势 | 能动态调查与恢复 | 成本可控、可并行、易复现 |
| 风险 | 循环、迷路、长轨迹误差 | 阶段错误难以跨阶段回退 |
| 上下文 | 随交互逐步获得 | 分层压缩后批量提供 |
| 搜索 | 工具驱动、动态 | prompt + embedding |
| 验证 | agent 主动运行测试 | 多候选统一排序 |

现实系统往往采用混合方案：用结构化 pipeline 做默认路径，在定位置信度低、环境异常或验证冲突时，启动一个受预算约束的 agent 做动态调查。

## 9. 十分钟速读

1. 看 Figure 1，记住 Localization、Repair、Validation 三阶段。
2. 精读 skeleton format 和三层定位。
3. 看 Search/Replace patch 格式与多样本策略。
4. 精读 reproduction test 的生成、过滤和 patch selection。
5. 看三组消融：定位、repair sampling、validation。

## 10. 读完应该带走什么

Agentless 把 Code Agent 的重点从“如何让模型自主规划更久”转向三个更可操作的问题：**是否找对位置、候选集中是否有正确 patch、是否有可靠 verifier 把它选出来。** 这是构建低成本系统时非常强的基线。

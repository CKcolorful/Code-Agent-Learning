# SWE-agent：工具接口本身就是能力

论文：[SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering](https://arxiv.org/abs/2405.15793)

官方代码：[SWE-agent/SWE-agent](https://github.com/SWE-agent/SWE-agent)

项目文档：[swe-agent.com](https://swe-agent.com/)

## 一句话结论

在底层模型不变时，为模型设计更合适的搜索、文件浏览、编辑和反馈接口，就能显著提高仓库级修复能力；ACI 不是包装层，而是 agent 能力的一部分。

## 1. 什么是 ACI

人类使用 IDE，是因为 IDE 把复杂操作压缩为符合人类认知的交互。语言模型也有自己的限制：上下文昂贵、容易被冗余输出干扰、不擅长脆弱的行号算术、会重复错误操作。论文把面向模型设计的界面称为 **Agent-Computer Interface（ACI）**。

ACI 同时定义：

- 模型可以发出哪些命令；
- 每条命令怎样描述、解析和执行；
- 环境状态怎样反馈给模型；
- 历史 Observation 怎样裁剪并重新放入上下文；
- 错误动作怎样拦截、解释和恢复。

因此相同 LLM 配上不同 ACI，会表现得像不同能力等级的系统。

## 2. 四条设计原则

论文从轨迹观察和配置搜索中总结出四个原则：

1. **动作简单、容易理解**：参数少、文档短、格式稳定。
2. **动作紧凑而高效**：一次完成有意义的高阶操作，避免多轮拼装脆弱 shell 命令。
3. **反馈信息充分但简洁**：告诉模型动作产生了什么变化，同时严格限制无关输出。
4. **Guardrail 阻止错误扩散**：语法检查、失败回滚和明确错误消息帮助模型快速恢复。

这四条存在张力。例如搜索最多只返回有限结果能保护上下文，却可能迫使模型再调用一次；一个“万能 shell”动作自由度高，却把命令语法、输出过滤和编辑正确性全丢给模型。

## 3. SWE-agent 提供了哪些关键接口

### 搜索与导航

shell-only agent 常用 `cd -> ls -> grep -> cat`，步骤多且输出不可控。SWE-agent 提供面向仓库的文件查找、目录搜索和文件内搜索，限制结果数量，让 agent 更快从文件级定位到行级位置。

### File Viewer

直接 `cat` 大文件会淹没上下文。File Viewer 以固定窗口显示带行号的代码，并提供打开、跳转、上下滚动等稳定动作。论文消融中，100 行窗口优于 30 行窗口和整文件：太少会失去局部语义，太多会增加干扰。

### File Editor

专用 `edit` 动作与当前 viewer 内容对齐，支持一次多行修改。修改后立即显示变更附近代码；若引入 lint/语法错误则拒绝或回滚，并同时给出错误、拟修改内容和原始内容。这比让模型拼 `sed` 参数可靠得多。

### 执行与测试

agent 可以写复现脚本、运行 Python/pytest、观察失败，再进入“编辑—执行—再编辑”循环。论文对轨迹的统计显示，成功工作流早期主要是复现和定位，中后期则反复交替编辑与测试。

### Context Management

SWE-agent 不盲目保留所有长输出，而会折叠旧 Observation。论文消融中，“最近 5 个 Observation”优于完整历史，说明更多上下文并不自动等于更多有效状态。

## 4. 论文实验说明了什么

使用 GPT-4 Turbo 时，论文报告 SWE-agent 在完整 SWE-bench 上解决 12.47%，在 SWE-bench Lite 上解决 18.0%；同一模型的 shell-only agent 在 Lite 上为 11.0%。论文将这视为 ACI 带来的 64% 相对提升。

更有解释力的是消融实验：

- 去掉专用 editor，Lite 结果从 18.0% 降至 10.3%；
- 保留 edit 但去掉 lint guardrail，降至 15.0%；
- 使用迭代式、冗长搜索，降至 12.0%；
- 显示整文件，降至 12.7%；
- 保留完整历史，降至 15.0%。

这些数字属于论文当时的模型和配置，但揭示的方向稳定：**工具的可用性、Observation 密度和错误恢复会直接改变模型的有效能力。**

## 5. Agent 为什么会迷路、循环或过早提交

- **迷路**：搜索结果过宽，viewer 窗口缺少位置感，历史中充满旧代码片段。
- **循环**：环境没有明确告诉模型“这个动作和上次相同且没有进展”。
- **编辑漂移**：模型依据几轮前的文件内容生成修改，行号和当前状态不一致。
- **过早提交**：预算即将耗尽，或接口允许 `submit` 而没有强制验证门禁。
- **错误恢复成本高**：静默编辑只返回成功/失败，不展示错误原因和当前文件状态。

ACI 的目标不是替模型完成推理，而是让正确路径短、错误路径可见、恢复路径便宜。

## 6. 一个现代 ACI 的最小规格

```text
search(query, path?, max_results)
  -> 命中文件、行号、有限上下文、是否截断

view(path, start_line, end_line)
  -> 带稳定行号的代码窗口、文件总行数

edit(path, expected_old_text, new_text)
  -> 是否唯一匹配、diff、语法检查、失败时不落盘

run(command, timeout, max_output)
  -> exit code、stdout/stderr 摘要、截断标记、耗时

diff()
  -> 当前所有修改及统计

submit()
  -> 只有在存在 diff 且验证门禁满足时成功
```

还应加入：命令超时、危险操作限制、输出去重、同动作循环检测、编辑前版本校验、测试结果结构化、每类工具的预算统计。

## 7. 怎样读源码

不要只找 prompt。按数据流跟一遍：

1. 配置如何声明工具与文档；
2. LLM 输出如何被解析成 action；
3. action 如何进入 Docker 环境执行；
4. stdout 怎样转成 Observation；
5. 历史如何被裁剪后组成下一次模型输入；
6. 最终 patch 和 trajectory 如何记录并交给 SWE-bench 评测。

论文把系统分为 environment、agent、logging 三部分，这个拆分仍适合作为小型实现的起点。

## 8. 十分钟速读

1. 看 Figure 2，先理解 ACI 与人类 UI 的类比。
2. 精读 ACI 四原则和 Section 3。
3. 看 Table 3 消融，特别是 editor、viewer、history。
4. 看附录中的 viewer/editor 输出示例和错误恢复消息。
5. 读 trajectory phases，观察“复现—定位—编辑—测试—提交”的自然结构。

## 9. 读完应该带走什么

SWE-agent 最重要的观点是：**不要只问模型够不够强，还要问环境是否用模型能稳定理解的方式暴露了状态和操作。** 对 Code Agent 来说，一条可靠的 edit、一段恰到好处的文件窗口、一个可诊断的失败 Observation，常常比再加一段复杂 prompt 更有价值。
